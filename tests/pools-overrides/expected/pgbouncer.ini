# rendered by docker-pgbouncer, regenerated on every start
################## Auto generated ##################
[databases]
base = auth_user=postgres dbname=appdb host=db.example.com pool_size=20 port=5432
replica = auth_user=readonly client_encoding=UTF8 dbname=reporting host=replica.example.com max_db_connections=8 min_pool_size=1 password=r3ad0nly pool_mode=session pool_size=5 port=6543 reserve_pool=2 user=readonly
[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 5432
unix_socket_dir = 
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = md5
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres



# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts
################## end file ##################
