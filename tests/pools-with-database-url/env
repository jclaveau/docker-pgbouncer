# boot: yes
# A single DATABASE_URL is a way of spelling the connection defaults, so every
# pool inherits its host, port, database and user unless it overrides them.
DATABASE_URL=postgres://appuser:s3cret@url-host.example.com:6543/urldb
POOLS=base,paid
POOL_PAID_POOL_SIZE=34
POOL_PAID_DBNAME=reporting
