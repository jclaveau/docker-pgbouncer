# rendered by docker-pgbouncer, regenerated on every start
################## Auto generated ##################
[databases]
appdb = host=db.example.com port=5432 auth_user=lookup
[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 5432
unix_socket_dir = 
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = md5
auth_user = lookup
auth_query = SELECT usename, passwd FROM pg_shadow WHERE usename=$1
ignore_startup_parameters = extra_float_digits

# Log settings
admin_users = postgres



# Connection sanity checks, timeouts

# TLS settings

# Dangerous timeouts
################## end file ##################
