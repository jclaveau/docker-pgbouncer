# boot: yes
# The credentials must not become the identity the entries authenticate as:
# every pool here answers to appuser, not to the last name in USERS.
# No pool forces a user, so reporting must exist on the server too: pgbouncer
# opens the server connection as whoever authenticated.
DB_HOST=db.example.com
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=s3cret
POOLS=base,paid
POOL_PAID_POOL_SIZE=34
USERS=reporting
USER_REPORTING_PASSWORD=r3port
