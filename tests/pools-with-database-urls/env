# boot: yes
# DATABASE_URLS feeds the userlist only; the entries come from POOLS, and the
# defaults from DB_*, so a pool needs its own host or DB_HOST.
DATABASE_URLS=postgres://appuser:s3cret@db1.example.com:5432/first,postgres://otheruser:0ther@db2.example.com:5432/second
DB_HOST=db.example.com
DB_NAME=appdb
POOLS=base,paid
POOL_PAID_POOL_SIZE=34
