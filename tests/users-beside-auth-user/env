# boot: yes
# The listed credentials are verified straight from userlist.txt. Anyone else is
# looked up as AUTH_USER, which is the entry's auth_user while no DB_USER names one.
DB_HOST=db.example.com
DB_NAME=appdb
AUTH_USER=lookup
USERS=app,reporting
USER_APP_PASSWORD=s3cret
USER_REPORTING_PASSWORD=r3port
